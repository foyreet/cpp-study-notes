/*
 p - является указателем на тип int
 if (p) // если адрес не нулевой p != nullptr, тогда true, если p == nullptr тогда false
 if (*p) // если значение p не нулевое тогда true, если 0 тогда false
 * */
